# Test-only isolation. Original pool sizes and timeouts are inherited unchanged.
spring.datasource.url=${CAPACITY_DB_URL:jdbc:mysql://127.0.0.1:13306/mformusic_capacity?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${CAPACITY_DB_USER:capacity}
spring.datasource.password=${CAPACITY_DB_PASSWORD:capacity-local-only}
spring.jpa.hibernate.ddl-auto=update
jwt.secret=capacity-test-only-signing-key-never-use-in-production-123456789
supabase.url=http://127.0.0.1:1
supabase.anonKey=unused-test-only
mlops.fastapi.url=http://capacity-fastapi.invalid
mlops.fastapi.enabled=true
kafka.telemetry.enabled=false
spring.kafka.admin.auto-create=false
server.address=127.0.0.1
server.port=18080
server.tomcat.mbeanregistry.enabled=true
capacity.saavn-delay-ms=150
capacity.fastapi-delay-ms=50
capacity.upload-delay-ms=250
capacity.seed-users=500
capacity.seed-songs=2000
